package com.litsorbeklik.app

import android.app.Application

class LitsorBeKlikApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Supabase client, crash reporting, etc. would be initialized here.
    }
}
